{
  "properties": {
    "jsonrpc": {"type":"string", "pattern":"^2\\.0$"},
    "id":      {"type": "number"}
  },
  "required": ["jsonrpc", "method", "params", "id"],
  "oneOf": [
    {
      "properties":{
        "method": {"type":"string", "pattern": "^(encode|decode)$"},
        "params": { "type":"array", "minItems":2, "maxItems": 2,
          "items": [{"type":"number", "minimum": 1, "maximum": 25}, {"type":"string"}]}
      }
    },
    {
      "properties":{
        "method": {"type":"string", "pattern": "^reset$"},
        "params": { "type":"array", "minItems": 0, "maxItems": 0}
      }
    }
  ]
}
